# 🖥️ Veritas Client

The user interface for the **Veritas** misinformation detection system. Built with React.js.

## 🚀 Quick Start

From this directory:

```bash
npm install
npm start
```

Runs the app in development mode at [http://localhost:3000](http://localhost:3000).

## 🔌 Connection
This client expects the **Veritas Server** to be running on `http://localhost:5001`.
If the server is not running, the analysis features will fail.

> Please refer to the [Main Project README](../README.md) for full architecture and setup instructions.
